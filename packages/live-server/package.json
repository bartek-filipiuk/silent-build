{
  "name": "@silent-build/live-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "silent-build-live": "./dist/cli.js"
  },
  "scripts": {
    "start": "tsx src/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@silent-build/harvester": "workspace:*",
    "@silent-build/shared": "workspace:*",
    "chokidar": "^5.0.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}

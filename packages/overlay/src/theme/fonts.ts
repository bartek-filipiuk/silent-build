/**
 * Font loading for silent-build overlay.
 *
 * Placeholder: MVP uses system monospace only. Claude Design phase will
 * populate this with @remotion/google-fonts loadFont() calls for
 * Space Grotesk (sans) and JetBrains Mono (mono).
 */

export function loadFonts(): void {
  // Intentionally empty in current phase.
  // Future: call loadFont() from @remotion/google-fonts here.
}
